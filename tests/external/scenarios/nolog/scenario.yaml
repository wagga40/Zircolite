# -n suppresses the log file and the detections file; the process should still exit 0
tags: [json, cli]
command:
  - "-e"
  - "/data/input/events.jsonl"
  - "-r"
  - "/data/input/ruleset.json"
  - "-j"
  - "-n"
expected_exit_code: 0
